apiVersion: v1
kind: Service
metadata:
  name: streamlit-service
  labels:
    app: streamlit-app
spec:
  type: NodePort
  selector:
    app: streamlit-app
  ports:
  - name: http
    port: 8501
    targetPort: 8501
    nodePort: 30501
